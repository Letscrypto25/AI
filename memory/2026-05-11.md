- Active project worked on today: `C:\Users\ethan\Desktop\LC TOOLS\Projects\AAA` (git branch `aaa`).
- Ethan wants completed AAA coding changes committed and pushed to git when the work is properly finished and checked.
- Useful AAA environment note on this Windows machine: frontend builds can fail after install if optional native packages are missing. The ones that mattered today were `@rollup/rollup-win32-x64-msvc`, `lightningcss-win32-x64-msvc`, and `@tailwindcss/oxide-win32-x64-msvc`. After restoring those, `corepack pnpm --filter @workspace/aaa-bets run build` succeeded.
- Active project worked on today: `C:\Users\ethan\Desktop\LC TOOLS\Projects\AAA` (git branch `aaa`).
- Ethan wants completed AAA coding changes committed and pushed to git when the work is properly finished and checked.
- Useful AAA environment note on this Windows machine: frontend builds can fail after install if optional native packages are missing. The ones that mattered today were `@rollup/rollup-win32-x64-msvc`, `lightningcss-win32-x64-msvc`, and `@tailwindcss/oxide-win32-x64-msvc`. After restoring those, `corepack pnpm --filter @workspace/aaa-bets run build` succeeded.
- Crypto bot repo worked on today: `C:\Users\ethan\Saved Games\Crypto-bot-2`.
- Crypto bot Railway deploy detail worth remembering: the backend can crash on startup if the built CommonJS server still relies on `createRequire(import.meta.url)` inside `server/instrument.js`. The fix was pushed in commit `fe653e4` (`fix: restore crypto bot Railway startup`).
- Local validation for that crypto bot fix succeeded with `npm run build:server`, `npm run start:prod`, and a healthy `http://127.0.0.1:8080/api/health` response.
- Railway CLI on this machine was unauthorized during the crypto bot recovery, so deploy inspection/redeploy from CLI still requires `railway login` or using the Railway dashboard.
- Active project worked on today: `C:\Users\ethan\Desktop\LC TOOLS\Projects\AAA` (git branch `aaa`).
- Ethan wants completed AAA coding changes committed and pushed to git when the work is properly finished and checked.
- Useful AAA environment note on this Windows machine: frontend builds can fail after install if optional native packages are missing. The ones that mattered today were `@rollup/rollup-win32-x64-msvc`, `lightningcss-win32-x64-msvc`, and `@tailwindcss/oxide-win32-x64-msvc`. After restoring those, `corepack pnpm --filter @workspace/aaa-bets run build` succeeded.
- Active project worked on today: `C:\Users\ethan\Desktop\LC TOOLS\Projects\AAA` (git branch `aaa`).
- Ethan wants completed AAA coding changes committed and pushed to git when the work is properly finished and checked.
- Useful AAA environment note on this Windows machine: frontend builds can fail after install if optional native packages are missing. The ones that mattered today were `@rollup/rollup-win32-x64-msvc`, `lightningcss-win32-x64-msvc`, and `@tailwindcss/oxide-win32-x64-msvc`. After restoring those, `corepack pnpm --filter @workspace/aaa-bets run build` succeeded.
- Crypto bot repo worked on today: `C:\Users\ethan\Saved Games\Crypto-bot-2`.
- Crypto bot Railway deploy detail worth remembering: the backend can crash on startup if the built CommonJS server still relies on `createRequire(import.meta.url)` inside `server/instrument.js`. The fix was pushed in commit `fe653e4` (`fix: restore crypto bot Railway startup`).
- Local validation for that crypto bot fix succeeded with `npm run build:server`, `npm run start:prod`, and a healthy `http://127.0.0.1:8080/api/health` response.
- Railway CLI on this machine was unauthorized during the crypto bot recovery, so deploy inspection/redeploy from CLI still requires `railway login` or using the Railway dashboard.
- AAA follow-up fix was pushed on branch `aaa` in commit `c07f309`; it tightened post-race result refresh behavior and reduced wrong-race chat focus drift.
- Durable AAA diagnosis: result delays were partly caused by the scheduler not actively chasing post-race result updates after jump time, and chat could infer the wrong focus race when the user's message was ambiguous.
- AAA learning status: the app already has adaptive learning/calibration through stored forecast snapshots, official race results, graded predictions, and `learningFeedbackTable` adjustments, but it is not a separate retrained ML model yet.
- The AAA follow-up server checks passed with `corepack pnpm --filter @workspace/api-server run typecheck` and `corepack pnpm --filter @workspace/api-server run build`.
- AAA prediction-quality follow-up was pushed on branch `aaa` in commit `a9d5e72` (`Tighten result sync and race focus`).
- Durable AAA diagnosis from that pass: races could become permanently ungraded if Tote showed an official result before the winner could be mapped to a stored runner, because the app could stop retrying too early; the fix keeps those races retryable until the official result is actually recorded.
- Durable AAA focus/context improvement: race search context now carries runner numbers and `#<number> <horse>` labels, chat focus inference scores venue/day/race-number/runner-number signals together, and the focus race briefing now explicitly tells AI to treat runner numbers as the source of truth.
- Durable AAA ranking/calibration improvement: forecast generation now blends AI output with deterministic fallback scoring keyed by runner identity, which reduces wrong-runner mapping and softens overconfident rankings instead of trusting raw AI ordering.
- The AAA checks for that pass passed with `corepack pnpm --filter @workspace/api-server run typecheck`, `corepack pnpm --filter @workspace/api-server run build`, `corepack pnpm --filter @workspace/aaa-bets run typecheck`, and `corepack pnpm --filter @workspace/aaa-bets run build`.
- Active project worked on today: `C:\Users\ethan\Desktop\LC TOOLS\Projects\AAA` (git branch `aaa`).
- Ethan wants completed AAA coding changes committed and pushed to git when the work is properly finished and checked.
- Useful AAA environment note on this Windows machine: frontend builds can fail after install if optional native packages are missing. The ones that mattered today were `@rollup/rollup-win32-x64-msvc`, `lightningcss-win32-x64-msvc`, and `@tailwindcss/oxide-win32-x64-msvc`. After restoring those, `corepack pnpm --filter @workspace/aaa-bets run build` succeeded.
- Active project worked on today: `C:\Users\ethan\Desktop\LC TOOLS\Projects\AAA` (git branch `aaa`).
- Ethan wants completed AAA coding changes committed and pushed to git when the work is properly finished and checked.
- Useful AAA environment note on this Windows machine: frontend builds can fail after install if optional native packages are missing. The ones that mattered today were `@rollup/rollup-win32-x64-msvc`, `lightningcss-win32-x64-msvc`, and `@tailwindcss/oxide-win32-x64-msvc`. After restoring those, `corepack pnpm --filter @workspace/aaa-bets run build` succeeded.
- Crypto bot repo worked on today: `C:\Users\ethan\Saved Games\Crypto-bot-2`.
- Crypto bot Railway deploy detail worth remembering: the backend can crash on startup if the built CommonJS server still relies on `createRequire(import.meta.url)` inside `server/instrument.js`. The fix was pushed in commit `fe653e4` (`fix: restore crypto bot Railway startup`).
- Local validation for that crypto bot fix succeeded with `npm run build:server`, `npm run start:prod`, and a healthy `http://127.0.0.1:8080/api/health` response.
- Railway CLI on this machine was unauthorized during the crypto bot recovery, so deploy inspection/redeploy from CLI still requires `railway login` or using the Railway dashboard.
- Active project worked on today: `C:\Users\ethan\Desktop\LC TOOLS\Projects\AAA` (git branch `aaa`).
- Ethan wants completed AAA coding changes committed and pushed to git when the work is properly finished and checked.
- Useful AAA environment note on this Windows machine: frontend builds can fail after install if optional native packages are missing. The ones that mattered today were `@rollup/rollup-win32-x64-msvc`, `lightningcss-win32-x64-msvc`, and `@tailwindcss/oxide-win32-x64-msvc`. After restoring those, `corepack pnpm --filter @workspace/aaa-bets run build` succeeded.
- Active project worked on today: `C:\Users\ethan\Desktop\LC TOOLS\Projects\AAA` (git branch `aaa`).
- Ethan wants completed AAA coding changes committed and pushed to git when the work is properly finished and checked.
- Useful AAA environment note on this Windows machine: frontend builds can fail after install if optional native packages are missing. The ones that mattered today were `@rollup/rollup-win32-x64-msvc`, `lightningcss-win32-x64-msvc`, and `@tailwindcss/oxide-win32-x64-msvc`. After restoring those, `corepack pnpm --filter @workspace/aaa-bets run build` succeeded.
- Crypto bot repo worked on today: `C:\Users\ethan\Saved Games\Crypto-bot-2`.
- Crypto bot Railway deploy detail worth remembering: the backend can crash on startup if the built CommonJS server still relies on `createRequire(import.meta.url)` inside `server/instrument.js`. The fix was pushed in commit `fe653e4` (`fix: restore crypto bot Railway startup`).
- Local validation for that crypto bot fix succeeded with `npm run build:server`, `npm run start:prod`, and a healthy `http://127.0.0.1:8080/api/health` response.
- Railway CLI on this machine was unauthorized during the crypto bot recovery, so deploy inspection/redeploy from CLI still requires `railway login` or using the Railway dashboard.
- AAA follow-up fix was pushed on branch `aaa` in commit `c07f309`; it tightened post-race result refresh behavior and reduced wrong-race chat focus drift.
- Durable AAA diagnosis: result delays were partly caused by the scheduler not actively chasing post-race result updates after jump time, and chat could infer the wrong focus race when the user's message was ambiguous.
- AAA learning status: the app already has adaptive learning/calibration through stored forecast snapshots, official race results, graded predictions, and `learningFeedbackTable` adjustments, but it is not a separate retrained ML model yet.
- The AAA follow-up server checks passed with `corepack pnpm --filter @workspace/api-server run typecheck` and `corepack pnpm --filter @workspace/api-server run build`.
- AAA prediction-quality follow-up was pushed on branch `aaa` in commit `a9d5e72` (`Tighten result sync and race focus`).
- Durable AAA diagnosis from that pass: races could become permanently ungraded if Tote showed an official result before the winner could be mapped to a stored runner, because the app could stop retrying too early; the fix keeps those races retryable until the official result is actually recorded.
- Durable AAA focus/context improvement: race search context now carries runner numbers and `#<number> <horse>` labels, chat focus inference scores venue/day/race-number/runner-number signals together, and the focus race briefing now explicitly tells AI to treat runner numbers as the source of truth.
- Durable AAA ranking/calibration improvement: forecast generation now blends AI output with deterministic fallback scoring keyed by runner identity, which reduces wrong-runner mapping and softens overconfident rankings instead of trusting raw AI ordering.
- The AAA checks for that pass passed with `corepack pnpm --filter @workspace/api-server run typecheck`, `corepack pnpm --filter @workspace/api-server run build`, `corepack pnpm --filter @workspace/aaa-bets run typecheck`, and `corepack pnpm --filter @workspace/aaa-bets run build`.
- AAA git tracking nuance on this machine: local branch `aaa` is tracking `origin/main` instead of `origin/aaa`, so `git status` can show `ahead of origin/main` even after the new AAA work has already been pushed to `origin/aaa`.
