# 2026-05-14

- AAA project repo path: `C:\Users\ethan\Desktop\LC TOOLS\Projects\AAA`.
- AAA branch setup is unusual: local branch `aaa` tracks `origin/main`, so `git status` ahead/behind counts can be misleading.
- For AAA branch syncs, the safe pattern is: push `aaa`, then replay the fix onto latest `origin/main` on a temp sync branch and push `HEAD:main` rather than force-pushing `main`.
- AAA was migrated to a 10-factor prediction weight model: `courseForm`, `formDistance`, `jockeyTrainer`, `oddsMovement`, `history`, `fieldStrength`, `weightCarried`, `surfaceFit`, `paceProfile`, `priceValue`.
- A key AAA production issue was DB/schema drift on `prediction_weights`; missing columns caused API startup failure and likely the blank Railway UI. The working fix path included updating code to the 10-factor model, regenerating API types, and pushing the DB schema.
- In this environment, repo scripts may need `corepack pnpm` because plain `pnpm` is not always available on PATH.
# 2026-05-14

- AAA project repo path: `C:\Users\ethan\Desktop\LC TOOLS\Projects\AAA`.
- AAA branch setup is unusual: local branch `aaa` tracks `origin/main`, so `git status` ahead/behind counts can be misleading.
- For AAA branch syncs, the safe pattern is: push `aaa`, then replay the fix onto latest `origin/main` on a temp sync branch and push `HEAD:main` rather than force-pushing `main`.
- AAA was migrated to a 10-factor prediction weight model: `courseForm`, `formDistance`, `jockeyTrainer`, `oddsMovement`, `history`, `fieldStrength`, `weightCarried`, `surfaceFit`, `paceProfile`, `priceValue`.
- A key AAA production issue was DB/schema drift on `prediction_weights`; missing columns caused API startup failure and likely the blank Railway UI. The working fix path included updating code to the 10-factor model, regenerating API types, and pushing the DB schema.
- In this environment, repo scripts may need `corepack pnpm` because plain `pnpm` is not always available on PATH.
- Crypto-bot-2 repo path: `C:\Users\ethan\Saved Games\Crypto-bot-2`.
- Crypto-bot-2 production 500s on `/api/users/subscription` and `/api/market/balance` were fixed by repairing Railway startup schema backfills and tightening subscription renewal reads; pushed to `main` as `359aa2d` (`Repair crypto bot production schema drift`).
- The production repair touched `server/index.js`, `server/routes/user.js`, and `server/services/subscription.ts`, and validated with `node --check`, `npm run typecheck`, `npm run build`, and `npm run build:server`.
