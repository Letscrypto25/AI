# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- - Crypto bot Railway deploy detail worth remembering: the backend can crash on startup if the built CommonJS server still relies on `createRequire(import.meta.url)` inside `server/instrument.js`. The fix was pushed in commit `fe653e4` (`fix: restore crypto bot Railway startup`). - Local validation for that crypto bot fix succeeded with `npm run build:server`, `npm run start:prod`, and a healthy `http://127.0.0.1:8080/api/health` response. - Railway CLI on this machine was unauthorized during the crypto bot recovery, so deploy inspection/redeploy from CLI still requires `railway login` or using the Railway dashboard. - AAA follow-up fix was pushed on branch `aaa` in commit `c07f309`; it tight [confidence=0.93 evidence=memory/2026-05-11.md:47-54]
- - Durable AAA focus/context improvement: race search context now carries runner numbers and `#<number> <horse>` labels, chat focus inference scores venue/day/race-number/runner-number signals together, and the focus race briefing now explicitly tells AI to treat runner numbers as the source of truth. - Durable AAA ranking/calibration improvement: forecast generation now blends AI output with deterministic fallback scoring keyed by runner identity, which reduces wrong-runner mapping and softens overconfident rankings instead of trusting raw AI ordering. - The AAA checks for that pass passed with `corepack pnpm --filter @workspace/api-server run typecheck`, `corepack pnpm --filter @workspace/a [confidence=0.93 evidence=memory/2026-05-11.md:56-60]
- - Crypto bot repo worked on today: `C:\Users\ethan\Saved Games\Crypto-bot-2`. - Crypto bot Railway deploy detail worth remembering: the backend can crash on startup if the built CommonJS server still relies on `createRequire(import.meta.url)` inside `server/instrument.js`. The fix was pushed in commit `fe653e4` (`fix: restore crypto bot Railway startup`). - Local validation for that crypto bot fix succeeded with `npm run build:server`, `npm run start:prod`, and a healthy `http://127.0.0.1:8080/api/health` response. - Railway CLI on this machine was unauthorized during the crypto bot recovery, so deploy inspection/redeploy from CLI still requires `railway login` or using the Railway dashboard [confidence=0.86 evidence=memory/2026-05-11.md:7-15]
