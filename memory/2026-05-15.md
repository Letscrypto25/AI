## Crypto-bot-2 durable note
- Repo: `C:\Users\ethan\Saved Games\Crypto-bot-2`
- On 2026-05-15, pushed `main` commit `4dc0d8d` `Fix Joe subscription visibility and read-only handling`.
- User-facing requirement clarified: keep two subscription plans, `Joe` and `All Access`.
- `Joe` should be the chat/dashboard-plus-settings plan, including Groq API, Luno/Binance credentials, trade size, and safety settings; `All Access` remains the fuller expert plan.
- Important production fix: subscription tier reads must not depend on writable DB state. The app should return both plans from code even if `subscription_tiers` is stale or the Railway DB is read-only.
- Activation paths now surface a cleaner read-only message instead of leaking raw DB write errors, but true activation still needs a writable production DB connection.
