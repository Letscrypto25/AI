# 2026-05-16

- Configured `C:\Users\ethan\Saved Games\ide\Projects\disjointed` for Railway single-service deployment backed by Supabase Postgres via `DATABASE_URL`; added `railway.json`, `Procfile`, and a populated `.env.example` covering required and optional runtime variables.
- `disjointed` was updated so the Express API can serve the built frontend in one Railway service, and the repo was pushed to `origin/main` at commit `4f5761a` (`Configure disjointed for Railway single server`).
