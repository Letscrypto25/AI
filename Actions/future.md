# future.md

Daily working schedule for `/ide`.

## Default cadence
- Heartbeat target: every 15 minutes.
- Review the request queue and action queue on each wake.
- Fall back to `scheduled-jobs.md` when no one-off job is active.

## Active items
- Keep the local `/ide` chat healthy and verify it still responds when needed.
- Keep `brain.md`, `Actions/`, and `mind/` aligned with workspace behavior.
- Hold request [9] at the current external blocker until Railway access is fixed and a real public app URL exists.

## One-off follow-ups
- Re-check Railway deployment readiness when the token/access state or public Railway URL changes.