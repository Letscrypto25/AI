kind = "web"
previewPath = "/"
title = "AAA Bets"
version = "1.0.0"
id = "artifacts/aaa-bets"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/aaa-bets run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/aaa-bets", "run", "build" ]
publicDir = "artifacts/aaa-bets/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
